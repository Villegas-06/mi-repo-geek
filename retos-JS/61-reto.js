// Setup
function testStrict(val) {
    if (val === 7) { // Change this line
      return "Equal";
    }
    return "Not Equal";
  }
  
  // Change this value to test
  testStrict(10);

  /* Nota: el triple igual se usa para comparar entre números y strings 
  para que la condición se cumpla correctamente */